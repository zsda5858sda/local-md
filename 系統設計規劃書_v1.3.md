# Local-first 區塊式 Markdown 編輯器 - 系統設計規劃書

**版本：** v1.3  
**日期：** 2026-08-07  
**文件用途：** 提供給 AI / 開發者作為實作依據，內容涵蓋需求定義、設計原則、技術架構、Markdown 相容範圍、Canonical 序列化風格、資料格式、功能規格、安全性、測試策略、開發階段與驗收標準。

## v1.3 變更摘要（相對 v1.2）

1. 將 round-trip 保證拆分為「Semantic Round-trip Guarantee」與「Raw Preservation Guarantee」，避免把語意保真與原始字串保留混為一談。
2. 明訂 Soft Break 在 v1 的 canonical 行為：一般段落內的 soft line break 視為語意空白，序列化時正規化為單一空白；Hard Break 仍以行尾反斜線序列化。
3. Loose / Tight List 改為保留語意狀態，不再要求還原來源文件的精確空白行分布。
4. 新增 MDAST Node Allowlist Validator，所有 parse 結果在進入 ProseMirror 前必須驗證 node / mark 是否屬於 v1 支援集合。
5. 補強 RawMarkdownNode 邊界規則：只有可獨立切割且不影響前後語意的 unsupported source range 才能降級為 RawMarkdownNode；否則整份文件進入 Compatibility Mode。
6. 修正清單縮排規則：取消「每層固定 2 個空白」，改依 CommonMark list content column 計算，避免多位數 ordered list 產生無效縮排。
7. 表格 canonical 規則改為保留 alignment semantics，而不是原始 delimiter 標記字串；pretty-print 採固定 Unicode display-width 計算規則。
8. 新增 FileFormatProfile，明確定義 Open Folder 的 encoding / BOM / EOL 保存策略，以及 Import / Export 的 UTF-8 + LF 正規化策略。
9. Filesystem watcher 改為 Workspace root recursive watcher；未開啟檔案的外部變更也必須更新 Workspace Tree 與全文搜尋索引。
10. Autosave 在 atomic replace 前再次驗證 expected disk hash，降低 TOCTOU race；新增 snapshot throttle，避免 1.5 秒 autosave 快速洗掉所有快照。
11. 全文搜尋改以 raw Markdown source 建立可定位的索引，避免去除 Markdown 語法後產生 source offset 對不上問題。
12. 新增 rename / move 與相對連結處理規則，避免移動文件後圖片或本機連結無聲失效。
13. Workspace 設定檔若 `version` 高於目前 App 支援版本，設定檔不得被舊版 App 覆寫。
14. 將 atomic write 與基本 disk version check 提前至 Phase 1，不再等到 Workspace Phase 才補可靠儲存。
15. 新增 property-based / fuzz round-trip test，補足固定 fixture 無法覆蓋的組合情境。
16. 明確限制 v1 的關閉保證為 graceful shutdown；非正常終止後的 session recovery 列為 v2，避免宣告無法實作的 crash recovery 保證。
17. 統一快照術語為 `.snapshots/`，不再以 `.bak` 稱呼本機快照機制。

# 1. 專案目標

開發一款**桌面端、本機優先（local-first）**的 Markdown 編輯器，定位介於 Notion 與 HackMD 之間：保留接近 Notion 的區塊式編輯體驗，但所有內容必須以標準、可攜的 Markdown 純文字檔儲存。

核心訴求：

1. 不內建任何 AI 功能。
2. 不做雲端同步、不做多人即時協作、不需要帳號系統。
3. 提供接近 Notion 的區塊式（block-based）編輯體驗與常用格式操作。
4. 支援直接開啟既有 Markdown 資料夾，以及批次匯入既有 `.md` / `.markdown` 檔案。
5. 儲存格式必須保持標準、可攜、可由其他文字編輯器直接開啟。
6. 對「本系統明確宣告支援」的 Markdown 語法，必須提供可靠的 semantic round-trip 保證，避免編輯後語意失真。
7. 對 RawMarkdownNode 與未修改的 YAML front-matter，必須提供明確的 raw preservation 保證。
8. 預設可在完全離線環境下運作，不主動向外部伺服器傳送文件內容。

## 1.1 非目標（Out of Scope）

v1 不實作以下功能：

- 多人即時協作。
- 雲端帳號與雲端同步。
- 伺服器端資料庫。
- AI 摘要、AI 續寫、AI 翻譯等功能。
- Notion database、relation、formula、synced block 等平台專屬資料模型。
- Obsidian wiki-link 等平台專屬語法。
- Mermaid、KaTeX 等需額外渲染引擎的語法；可列為 v2 選配功能。
- 任意 HTML 執行。
- 完整版本歷史（version history）；v1 僅提供 4.7.1 定義的有限本機快照備份。
- 非正常終止後的完整 Session Restore / Crash Recovery；可列為 v2 選配功能。

未來若加入 AI 或其他線上功能，必須以可完全關閉的外掛或獨立模組存在，不得改變核心 Markdown 儲存邏輯。

## 1.2 核心設計原則

### 原則 A：Markdown 檔案是持久化資料的唯一事實來源

- 使用者的 `.md` 檔案為文件內容的 Persistent Source of Truth。
- ProseMirror Document 僅為「目前開啟文件」的 Runtime Editor State。
- 不得把 ProseMirror JSON、SQLite、IndexedDB 或其他內部格式當成真正文件來源。

### 原則 B：檔案系統是 Workspace 結構的唯一事實來源

- Workspace 的資料夾與檔案結構，以實際 filesystem 為準。
- 設定檔不得保存一份獨立且可與 filesystem 發生衝突的完整 `fileTree`。

### 原則 C：不得靜默遺失資料

若系統遇到無法安全解析或重新序列化的語法，不得直接刪除、忽略或改寫成其他內容。

系統必須採取以下其中一種策略：

1. 保留為 RawMarkdownNode；或
2. 將文件切換為 Compatibility Read-only Mode；或
3. 拒絕匯入並清楚列出原因。

### 原則 D：序列化輸出必須具備確定性（Deterministic Output）

同一份 ProseMirror Document，在相同版本的系統、相同 serializer 設定與相同 FileFormatProfile 下，序列化結果必須可重現。

- Canonical Markdown Text 層必須產生完全一致的 UTF-8 logical text，內部 EOL 固定為 LF。
- 實際寫入磁碟時，Open Folder 可依 FileFormatProfile 將 Canonical Markdown Text 轉回來源 encoding / BOM / EOL；因此 byte-identical 保證以「相同 FileFormatProfile」為前提。
- Import Folder、Export 與新建檔案預設採 UTF-8（無 BOM）+ LF。
- 不得依賴套件隱含預設值，所有 serializer 參數必須顯式設定並記錄於第 3.7 節。

# 2. 技術架構

## 2.1 技術選型

| 層級 | 選用技術 | 說明 |
| --- | --- | --- |
| 桌面應用框架 | Tauri 2.x | Rust 後端 + Web 前端；負責桌面封裝、檔案系統與原生能力 |
| 前端框架 | React + TypeScript | UI 與應用狀態管理；本專案不再保留 React / Vue 二選一 |
| 編輯器核心 | Tiptap + ProseMirror | 負責編輯器文件模型、selection、transaction、快捷鍵與區塊操作 |
| Markdown Parser / Serializer | unified / remark 生態系 | `remark-parse`、`remark-gfm`、`remark-stringify`；Markdown 解析統一由此層負責，序列化參數固定依 3.7 節設定 |
| YAML | `yaml`（eemeli/yaml，支援保留 comment / order 的 Document API） | 用於 front-matter 解析與修改；未修改時優先原樣寫回 |
| 編碼偵測 | chardet 或等效函式庫 | 至少涵蓋 UTF-8 / UTF-8 BOM / Big5 / GBK / GB2312 / Shift-JIS，見 4.4.1 |
| 儲存方式 | 本機純文字 `.md` | 不以內嵌資料庫保存文件正文 |
| 檔案操作 | Tauri Rust API | 讀寫、atomic write、重新命名、recursive watch、路徑正規化 |
| 全文搜尋索引 | 記憶體內倒排索引（in-memory index） | Workspace 開啟時建立，不落地為資料庫，見 4.2.1 |
| 設定 | App 設定檔 + Workspace 設定檔 | 分離全域偏好與工作區偏好 |

## 2.2 架構分層

```text
┌──────────────────────────────────────────────────────────┐
│ UI Layer                                                 │
│ React: Sidebar / Editor / Toolbar / Dialog / Settings /  │
│        Search Panel                                      │
├──────────────────────────────────────────────────────────┤
│ Editor Runtime Layer                                     │
│ Tiptap / ProseMirror Document / Selection / Transaction  │
│ 每份開啟文件各自持有獨立 Editor Runtime，見 2.4.1       │
├──────────────────────────────────────────────────────────┤
│ Markdown Conversion Layer                                │
│ Markdown <-> MDAST <-> ProseMirror Document              │
│ Node Allowlist Validator + Canonical Rules               │
├──────────────────────────────────────────────────────────┤
│ Application Service Layer                                │
│ Workspace / Import / Export / Asset / Conflict / Search  │
│ FileFormat / Snapshot / Link-Rewrite Service             │
├──────────────────────────────────────────────────────────┤
│ Filesystem Layer                                         │
│ Tauri Rust: read/write/watch/rename/path validation      │
└──────────────────────────────────────────────────────────┘
```

## 2.3 唯一 Markdown 轉換管線

系統必須只有一套 canonical Markdown parser / serializer，不得同時讓多套 Markdown engine 各自決定輸出格式。

### 載入流程

```text
File Bytes
  ↓
偵測 encoding / BOM / EOL，建立 FileFormatProfile
  ↓
Decode 為 Unicode，內部 EOL 正規化為 LF
  ↓
remark-parse + remark-gfm
  ↓
MDAST
  ↓
MDAST Node Allowlist Validator（3.5.2）
  ↓
mdastToProseMirror()
  ↓
ProseMirror Document
```

### 儲存流程

```text
ProseMirror Document
  ↓
proseMirrorToMdast()
  ↓
MDAST
  ↓
remark-stringify（固定參數，見 3.7）
  ↓
Canonical Markdown Text（LF）
  ↓
依 FileFormatProfile 決定 encoding / BOM / EOL
  ↓
Temporary File
  ↓
Expected Disk Hash 再驗證
  ↓
Atomic Replace
```

Tiptap / ProseMirror 不自行成為第二套 Markdown parser / serializer。

RawMarkdownNode 與未修改的 raw front-matter 為例外 preservation path：其內容不得先經過一般 MDAST semantic serializer 再重建。

## 2.4 Runtime 與 Persistent State

### Persistent State

- `.md` / `.markdown` 文件。
- 本機圖片與其他允許的附件。
- Workspace 設定檔。
- `.snapshots/` 快照檔（見 4.7.1）。

### Runtime State

- 目前開啟的 ProseMirror Document（可能有多份，見 2.4.1）。
- selection / cursor。
- undo / redo history。
- dirty state。
- `lastKnownDiskHash` / last known file version。
- `FileFormatProfile`。
- `saveGeneration`，用於協助辨識本 App 自身寫入事件。
- 全文搜尋索引（記憶體內，重啟後重建）。
- 暫時性 UI 狀態。

Runtime State 不得取代 Markdown 檔案成為永久資料來源。

## 2.4.1 多文件編輯模型

v1 採用分頁（Tab）式多文件編輯：

- 使用者可同時開啟多個文件，各自對應獨立的分頁。
- 每個分頁擁有獨立的 ProseMirror Document、dirty state、undo / redo history、`lastKnownDiskHash`、FileFormatProfile 與 autosave debounce timer。
- Autosave debounce（4.7 節）以「每個分頁獨立計時」運作，不共用單一計時器。
- Filesystem watcher 不以「每個分頁各建立一個 watcher」實作，而由 Workspace root recursive watcher 統一監聽並將事件路由至對應分頁、Workspace Tree 與 Search Index。
- 關閉分頁前若 dirty state 為 true，需比照 4.8 節的未儲存內容處理流程詢問使用者。
- 正常關閉應用程式（graceful shutdown）時，必須 flush 所有 dirty 分頁或明確提示使用者。
- 非正常終止（process kill、OS crash、斷電）後的 Session Restore / unsaved-buffer recovery 不屬於 v1 保證範圍，可列為 v2。

# 3. Markdown 語法規範

## 3.1 支援範圍聲明

本系統不宣稱完整支援 CommonMark 或完整 GFM。

v1 僅對本節明確列出的 CommonMark + GFM 受控語法子集提供 semantic round-trip 保證，且輸出一律遵循第 3.7 節 Canonical Serialization Style Guide。

## 3.2 v1 支援語法

### CommonMark 類型

- ATX 標題：`#` 至 `######`
- 段落。
- Soft line break / Hard line break（見 3.2.1 與 3.7.5、3.7.12）。
- 粗體：`**text**`
- 斜體：`*text*`
- 行內程式碼：`` `code` ``
- fenced code block：三個以上反引號，可含語言資訊。
- 無序清單。
- 有序清單（含自訂起始編號，見 3.7.6）。
- 巢狀清單。
- 引言：`>`
- 分隔線：`---`
- 超連結：`[text](url)`
- 圖片：`![alt](url)`
- Markdown escape / 跳脫字元。

### GFM 擴充

- 表格（對齊與欄寬規則見 3.7.7）。
- 任務清單：`- [ ]` / `- [x]`
- 刪除線：`~~text~~`
- GFM autolink。

## 3.2.1 Soft Break 與 Hard Break Mapping

為避免 ProseMirror runtime model 與 Markdown source line break 之間產生不明確 mapping，v1 固定採以下規則：

- **Soft Break**：一般段落中單純由原始換行形成、但不具有 hard break 語意的換行，視為「語意空白」。載入後不要求保留原始 source LF 位置；canonical 序列化時正規化為單一 ASCII 空白。
- **Hard Break**：必須在 ProseMirror 中使用明確的 `hardBreak` node 表示，序列化時固定輸出為「行尾反斜線 + LF」。
- 使用者在 WYSIWYG 編輯器中按一般 Enter 建立新段落，不等同 Soft Break；Soft Break 僅是來源 Markdown 的 inline whitespace normalization 規則。
- 若未來需要完整保留 soft break 的 source 位置，必須新增專用 source-aware node / token model，列為 v2，不得在 v1 暗示已支援 source-level preservation。

## 3.3 HTML Compatibility Extensions

v1 可選擇支援以下 HTML 相容擴充：

- 底線：`<u>text</u>`
- 摺疊區塊：`<details><summary>...</summary>...</details>`

規則：

1. 必須在設定或匯出介面中明確標示為「HTML Compatibility Extension」。
2. HTML 僅允許白名單標籤與安全屬性。
3. 禁止 `<script>`、`iframe`、event handler、任意 style / script injection 等可執行內容。
4. Strict Export 模式需提供降級策略。

## 3.4 v1 明確不支援

- Notion database / synced block。
- 自訂 callout / admonition 語法。
- Obsidian wiki-link。
- Mermaid。
- KaTeX / LaTeX math block。
- 任意 raw HTML（白名單標籤以外）。
- TOML 或其他非 YAML 格式的 front-matter（見 4.6.1）。
- Reference-style link / definition。
- 未在 3.2 / 3.3 列出的平台專屬 Markdown extension。

## 3.5 Unsupported Syntax 處理

偵測到不支援語法時，不得 silently drop。

優先策略：

1. 可辨識、可完整取得 source range、且能獨立原樣保存：轉為 RawMarkdownNode，編輯器以「原始 Markdown 區塊」呈現。
2. 無法安全切割或 round-trip：文件進入 Compatibility Read-only Mode。
3. 批次匯入時：在匯入報告列出檔案、行號、語法類型及處理結果。

## 3.5.1 RawMarkdownNode 的可編輯性與復原流程

- RawMarkdownNode 在編輯器中以純文字區塊形式顯示（例如等寬字型、灰底），使用者可以直接編輯其原始 Markdown 文字內容，但編輯器不對其內容做語法解析或 WYSIWYG 渲染。
- 使用者對 RawMarkdownNode 的文字修改，於儲存時原樣寫回對應區段，不經過 MDAST 語意轉換。
- 每個 RawMarkdownNode 需標註「不支援原因」（例如：Obsidian wiki-link、未知 HTML 標籤），供使用者於 UI 上檢視。
- 只有當 unsupported syntax 能以完整 source range 獨立切割，且該 range 不與前後節點存在跨區塊語意依賴時，才允許轉為 RawMarkdownNode。
- 若 unsupported syntax 會影響其他區塊語意，例如 reference-style link definition 被其他段落引用、跨區塊 HTML 結構、無法安全判定起訖範圍，則不得局部降級，整份文件必須進入 Compatibility Read-only Mode。

Compatibility Read-only Mode 復原流程：

1. **以純文字模式編輯**：允許使用者直接編輯整份原始 Markdown 文字（略過區塊編輯器），存檔前執行 syntax / safety preflight；不得將純文字模式內容強制轉回 ProseMirror 後再存檔。
2. **匯出為新檔案後手動處理**：不修改原檔，複製一份供使用者以其他工具處理。
3. **查看衝突詳情**：列出導致無法安全解析的具體語法與行號。

使用者不得在完全不知情的情況下被鎖在唯讀模式無法退出。

## 3.5.2 MDAST Node Allowlist Validator

所有 Markdown 在 `remark-parse + remark-gfm` 後、轉換為 ProseMirror Document 前，都必須通過 allowlist 驗證。

規則：

- v1 支援的 MDAST node / mark 類型必須建立明確 allowlist，不得以「remark 能 parse」等同「本 App 支援」。
- Validator 必須遞迴檢查整棵 MDAST，包括 block、inline、definition、HTML、table extension 等節點。
- 發現不在 allowlist 的節點時，依 3.5 的 RawMarkdownNode / Compatibility Mode 規則處理。
- Validator 必須保留位置資訊（line / column / offset）供匯入報告與 Compatibility 詳情使用。
- 新增 Markdown 功能時，PR 必須同時更新：allowlist、MDAST <-> ProseMirror mapping、serializer、fixture 與 round-trip test；不得只新增 UI node。

## 3.6 Round-trip 保證

基本測試流程：

```text
Markdown
  ↓
MDAST
  ↓
Allowlist Validator
  ↓
ProseMirror Document
  ↓
MDAST
  ↓
Canonical Markdown
```

### 3.6.1 兩層保證模型

#### A. Semantic Round-trip Guarantee

對 3.2 / 3.3 明確宣告支援的語法：

- `parse(original) -> ProseMirror -> serialize -> parse(serialized)` 後，兩端的 normalized semantic AST 必須等價。
- AST 比較時應忽略 source position、原始 marker 字元、非語意 whitespace 等 canonicalization 資訊，但不得忽略節點類型、階層、文字內容、link target、code 內容、table cell、list start、task checked state、alignment semantics 等語意資料。

#### B. Raw Preservation Guarantee

以下內容若未經使用者修改，必須走 preservation path，不得由一般 serializer 重建：

- RawMarkdownNode 的 raw source。
- 未修改的 YAML front-matter `rawFrontMatter`。

Raw Preservation 允許的唯一檔案層差異，是由 4.3 / 4.4 定義的 FileFormatProfile encoding / EOL policy 所造成的必要轉碼；內容字元本身不得靜默改寫。

### 3.6.2 Loose List 與 Tight List 判定規則

- 系統解析時保留 CommonMark list / listItem 的 loose / tight 語意狀態，例如 MDAST `spread` 等可表達資訊。
- v1 **不保證還原來源文件中每一個空白行的精確位置**；canonical serializer 只需依相同 loose / tight 語意產生固定且可重現的空白行格式。
- 使用者透過編輯器 UI 主動改變清單結構，例如在 list item 中加入第二個 paragraph，系統依編輯後 AST 重新判定 loose / tight，此為預期行為，不視為 round-trip 失真。
- 若來源的空白行分布具有超出 MDAST 可表達範圍的特殊格式需求，該需求不屬於 v1 semantic round-trip 保證。

### 3.6.3 允許的非語意差異（明確列舉，不得任意擴大解釋）

Round-trip 測試僅允許以下差異視為「非語意差異」：

1. Canonical Markdown Text 的檔案結尾換行，最終固定為恰好 1 個 LF。
2. 一般段落中的 Soft Break 依 3.2.1 正規化為單一 ASCII 空白。
3. 一般 inline text 中、兩個文字 token 之間的連續 ASCII 空白可正規化為單一空白；**不適用**於行首縮排、清單結構、Hard Break、程式碼區塊、行內程式碼、RawMarkdownNode、HTML、table 結構、link destination 等具語意或 preservation 要求的區域。
4. 依 3.7 節 Canonical Style Guide 產生的 Markdown marker 正規化，例如無序清單來源使用 `*`，輸出統一為 `-`；來源使用 `_` emphasis，輸出統一為 `*`。
5. Table delimiter row、cell padding 與對齊空白可依 3.7.7 canonicalize，但欄數、cell 內容與 alignment semantics 必須一致。
6. 實際磁碟檔案的 encoding / BOM / EOL 只可依 FileFormatProfile policy 轉換；Open Folder 的 preserve policy 不得擅自 normalize，Import / Export 則依 4.3、4.4、4.9 規格正規化。

除上述項目外，任何節點消失、層級錯誤、連結目標改變、程式碼內容改變（含縮排與空白）、表格欄位錯位、list start 改變、task checked state 改變、loose / tight 語意非預期翻轉等，一律視為 round-trip 失敗。

## 3.7 Canonical Serialization Style Guide

本節鎖定 `remark-stringify` / `mdast-util-to-markdown` 的輸出規則，所有開發者與 AI 實作者必須遵循此設定，不得使用套件隱含預設值或自行調整。

| 項目 | 固定規則 |
| --- | --- |
| 3.7.1 清單項目符號（bullet） | 一律使用 `-` |
| 3.7.2 強調符號（emphasis） | 斜體一律使用 `*`，粗體一律使用 `**`；不使用 `_` |
| 3.7.3 程式碼區塊圍籬符號（fence） | 一律使用反引號；fence 長度依內容中最長連續反引號動態調整以避免衝突，不使用 `~` |
| 3.7.4 清單縮排 | **不得固定每層 2 個空白。** List marker 後預設 1 個空白，nested content 依 CommonMark list content column 計算；ordered list marker 位數增加時，子內容縮排必須隨 content column 移動。Serializer 應顯式使用穩定的 list item indent policy，例如 `listItemIndent: 'one'`，並以 fixture 驗證。 |
| 3.7.5 Hard Break | 一律序列化為行尾反斜線 `\` + LF，不依賴行尾兩個空白 |
| 3.7.6 有序清單起始編號 | 保留 AST 中的起始編號，例如從 `5.` 開始，不強制重置為 `1.` |
| 3.7.7 表格 | 保留 left / center / right / none 的 alignment semantics；delimiter row 由 serializer 依 canonical rule 重建，不要求保留來源 `:---:` 的 dash 數。Cell pretty-print 使用固定 Unicode display-width 演算法，需對 CJK、全形字元與 emoji 有一致結果；函式庫版本必須由 lockfile 固定。 |
| 3.7.8 分隔線 | 一律使用 `---` |
| 3.7.9 連結 | 一律使用行內連結 `[text](url)`；reference-style link 不屬於 v1 支援範圍 |
| 3.7.10 Canonical EOL | Canonical Markdown Text 內部一律使用 LF，結尾恰好 1 個 LF；實際 disk EOL 可再依 FileFormatProfile 轉換 |
| 3.7.11 標題 | 一律使用 ATX 樣式 `#`，不使用 Setext 標題 |
| 3.7.12 Soft Break | 一般段落內 soft line break 一律正規化為單一 ASCII 空白 |
| 3.7.13 行尾空白 | Serializer 不輸出無語意 trailing whitespace；Hard Break 已由反斜線表示 |

此表格為強制規範。任何影響 serializer 的 Pull Request 若未同步更新固定參數、fixture 與測試，視為未通過 Code Review。

# 4. 功能規格

## 4.1 編輯功能

| 功能分類 | 具體功能 |
| --- | --- |
| 文字格式 | 粗體、斜體、刪除線、行內程式碼、可選底線 |
| 標題 | H1 - H6；支援 `#` 快捷輸入 |
| 清單 | 無序、有序（含自訂起始編號）、任務清單、巢狀縮排 |
| 區塊 | 引言、分隔線、程式碼區塊 |
| 表格 | 插入 / 新增 / 刪除欄列、欄位對齊 |
| 連結與圖片 | 超連結、圖片、本機資源引用 |
| 結構操作 | `/` 斜線指令、拖曳區塊、升降層級 |
| 摺疊 | `<details>` 相容模式，可選 |
| 快捷鍵 | Ctrl/Cmd+B、Ctrl/Cmd+I 等常見操作 |

所有 UI 編輯功能都必須有明確的 Markdown / 相容格式對應，否則不得加入核心編輯器。

## 4.2 Workspace 模式

使用者可選擇一個資料夾作為 Workspace。

側邊欄依 filesystem 即時顯示資料夾與 Markdown 檔案。

支援：

- 新增 Markdown 檔案。
- 新增資料夾。
- 刪除。
- 重新命名。
- 拖曳移動。
- 搜尋檔名。
- 全文搜尋（見 4.2.1）。
- 重新整理 filesystem 狀態。

Workspace Tree 每次以 filesystem 掃描結果建立，不以 JSON 內的 `fileTree` 作為真實來源。

## 4.2.1 全文搜尋

- 開啟 Workspace 時，於記憶體中建立所有 `.md` / `.markdown` 檔案的倒排索引，不落地儲存、不建立資料庫檔案。
- v1 搜尋的來源以 **raw Markdown source text** 為準，不先移除 Markdown 語法符號。使用者搜尋 `title` 仍可直接命中 `# title`，因此不需要先建立「去 Markdown 後的正文」才達成文字搜尋。
- 每筆搜尋索引至少保留：`filePath`、`lineNumber`、`sourceStartOffset`、`sourceEndOffset`、`rawLineText`，以確保搜尋結果能精準捲動與高亮原始檔位置。
- 支援基本關鍵字比對與大小寫不敏感搜尋；正則表達式搜尋列為 v2 選配功能。
- 搜尋結果需顯示命中檔案路徑、命中行的上下文片段；點擊後開啟對應分頁並捲動、高亮至 source range。
- UI 可對顯示片段進行輕量格式化，但不得因此改變索引定位的 source offset。
- 檔案內容於 Editor 修改或 recursive filesystem watcher 偵測到外部變更時，須增量更新索引。
- 索引更新失敗不得阻塞編輯器主要功能，僅降級為「該檔案暫不可搜尋」並記錄警告。

## 4.2.2 Rename / Move 與相對連結

檔案或資料夾 rename / move 可能改變相對路徑，因此不得只移動 filesystem entry 而不處理引用關係。

規則：

1. 對被移動 Markdown 文件本身的 outgoing local links / image links，系統在 move transaction 中重新計算相對路徑，目標仍應指向移動前相同的檔案或 asset。
2. 對 Workspace 內其他 Markdown 文件指向「被移動檔案」的 incoming relative links，系統需先掃描受影響引用並顯示數量 / 檔案清單。
3. incoming links 的批次改寫必須經使用者確認；不得因單純 move 而在背景靜默改寫大量其他文件。
4. 使用者選擇「更新引用」時，所有會被修改的文件在寫入前需套用 snapshot / atomic write / conflict check。
5. 使用者選擇「不更新引用」時，move 可繼續，但 UI 必須列出可能失效的 references。
6. move / rewrite 任一核心步驟失敗時，不得留下部分更新而沒有報告；應盡可能 rollback，或至少提供完整失敗清單與可復原資訊。

## 4.3 Open Folder 與 Import Folder 必須分離

### A. Open Folder

用途：直接編輯既有 Markdown 資料夾。

原則：

- 不複製原始檔案。
- 不自動搬移圖片。
- 不因「開啟 / 掃描」而改變換行、BOM、編碼或任何文件內容。
- 只有在使用者實際編輯並儲存後才寫回檔案。
- 開啟檔案時建立 FileFormatProfile，記錄至少：`encoding`、`bom`、`eol`。
- 使用者儲存既有檔案時，預設 `openFolderFileFormatPolicy = preserve`：Canonical Markdown Text 先產生 LF logical text，再依 FileFormatProfile 轉回原 encoding / BOM / EOL。
- 若使用者輸入的新字元無法由來源編碼表示，例如 Big5 檔案加入無法編碼的 Unicode 字元，系統必須停止儲存並提示「轉換為 UTF-8」；不得採用 replacement character、忽略字元或其他 lossy encoding。
- 新建 Markdown 檔案預設為 UTF-8（無 BOM）+ LF。

### B. Import Folder

用途：將外部 Markdown 資料整理後複製到新的 Workspace。

Import 不得直接破壞來源資料夾。

- 目標 Workspace 的 imported Markdown 一律正規化為 UTF-8（無 BOM）+ LF。
- 來源 encoding / EOL 資訊只需保留在匯入報告，不要求在目標檔案延續。

## 4.4 批次匯入流程

1. 使用者選擇來源根目錄。
2. 使用者選擇目標 Workspace。
3. 遞迴掃描 `.md` / `.markdown`。
4. 保存完整相對路徑結構。
5. 對每個檔案執行：
   - 偵測文字編碼（見 4.4.1）。
   - Decode 至 Unicode。
   - 在目標 Workspace 轉為 UTF-8（無 BOM）。
   - 將 EOL 正規化為 LF。
   - 解析 YAML front-matter（非 YAML 格式依 4.6.1 處理）。
   - 掃描本機圖片引用。
   - 執行 MDAST allowlist / unsupported syntax 檢查。
   - 解析 Markdown -> MDAST -> ProseMirror，執行 semantic round-trip preflight。
6. 若 preflight 失敗，不覆蓋或產生不完整結果。
7. 匯入完成後顯示報告。

匯入報告至少包含：

- 成功數量。
- 失敗數量。
- 失敗檔案與原因。
- 編碼問題（含偵測到的原始編碼名稱與信心分數）。
- 找不到的圖片。
- unsupported syntax。
- Compatibility Mode 檔案。
- 非 YAML front-matter。

## 4.4.1 編碼偵測範圍

- 偵測順序與涵蓋範圍至少包含：UTF-8（含 BOM）、Big5、GBK / GB2312、Shift-JIS。
- 使用統計式編碼偵測函式庫（例如 chardet 或等效實作），並設定信心分數門檻；低於門檻時，標記為「編碼不確定」並在匯入報告中列出，交由使用者手動指定編碼後重新匯入該檔案。
- 不得在低信心情況靜默假設為 UTF-8 並可能產生亂碼寫入。
- Import 後檔案一律以 UTF-8（不含 BOM）寫入目標 Workspace。
- Open Folder 則依 4.3 的 FileFormatProfile preserve policy 處理，不套用 Import 的強制 UTF-8 規則。

## 4.5 Asset 管理

### Open Folder

- 保留既有圖片位置與相對路徑。
- 不自動重組 `_assets/`。

### Import Folder

本機圖片可複製到：

```text
_assets/<hash-prefix>/<content-hash>.<ext>
```

規則：

- 使用內容 hash 避免重名覆蓋。
- 相同圖片可重複利用同一份 asset。
- Markdown 圖片連結改寫為相對路徑。
- 找不到的圖片不刪除原 Markdown link，只標記為 Missing Asset。
- `http://` / `https://` 圖片不自動下載。
- 指向來源根目錄外的本機檔案，匯入前需明確提示使用者。

## 4.5.1 Asset 垃圾回收策略

- 系統不自動刪除 `_assets/` 內任何檔案，即使該資源已不再被任何 `.md` 文件引用，避免誤刪仍在使用中或使用者手動保留的資源。
- 提供「掃描孤兒資源」的手動工具：掃描整個 Workspace 中所有 `.md` 文件的圖片引用，比對 `_assets/` 實際內容，列出未被任何文件引用的檔案清單。
- 孤兒資源的刪除必須由使用者在檢視清單後手動確認執行，且刪除前提供清單匯出或復原提示，例如優先移至系統資源回收桶而非直接永久刪除。

## 4.6 Front-matter

Front-matter 顯示於獨立的「頁面屬性」面板，不顯示在一般正文編輯畫面。

內部需要保留：

```ts
{
  rawFrontMatter: string,
  yamlDocument: YamlDocument,
  parsedMetadata: object
}
```

規則：

- 使用者未修改 metadata 時，優先原樣寫回 `rawFrontMatter`。
- 未知欄位不得刪除。
- 盡量保留欄位順序與 comment。
- 修改 metadata 後才由 YAML Document 重新序列化。

## 4.6.1 非 YAML Front-matter 處理

- v1 僅解析以 `---` 分隔的 YAML front-matter。
- 偵測到 `+++`（TOML）或其他格式的 front-matter 時，一律視為 3.4 節定義的 Unsupported Syntax。
- 若該 front-matter 能以完整且獨立的 document-prefix source range 保存，可依 3.5 轉為 RawMarkdownNode 保留於文件最前端，不嘗試解析欄位、不顯示於「頁面屬性」面板。
- 若無法安全切割，則文件進入 Compatibility Mode。
- 匯入報告需明確標示此類檔案為「Front-matter 格式不支援，已保留原始內容」或「需 Compatibility Mode」。

## 4.7 自動存檔

採用 debounce，而非固定 interval。

預設：

```text
autoSaveDebounceMs = 1500
```

流程：

```text
Editor change
  ↓
dirty = true
  ↓
1500ms debounce
  ↓
讀取目前 disk hash，與 lastKnownDiskHash 比較
  ↓
一致
  ↓
serialize -> Canonical Markdown -> FileFormatProfile encode
  ↓
寫入 temporary file
  ↓
atomic replace 前再次讀取 expected disk hash
  ↓
仍一致
  ↓
依 snapshot policy 建立舊版快照
  ↓
atomic replace
  ↓
更新 lastKnownDiskHash / saveGeneration
  ↓
dirty = false
```

若任一次 disk hash 驗證發現檔案已變更，禁止 autosave 覆蓋，改進入 4.8 conflict handling。

注意：第二次 hash 檢查是降低 check / write 間 race 的必要措施；不得只在 debounce 開始時檢查一次就直接 overwrite。

## 4.7.1 本機快照備份

- 於即將覆蓋既有檔案前，將舊版檔案內容複製至 Workspace 內隱藏備份資料夾，例如 `.snapshots/<相對路徑>/<timestamp>.md`。
- 每個檔案僅保留最近 N 份快照，預設 `snapshotRetentionCount = 5`。
- 為避免 1.5 秒 autosave 快速洗掉所有快照，autosave 觸發的快照建立需設最短間隔，預設 `snapshotMinIntervalMs = 60000`。
- 若距離上一份快照不足最短間隔，autosave 可直接寫入而不新增快照，但仍需執行 hash check 與 atomic write。
- 使用者主動執行「保留 Editor 版本並覆蓋外部版本」、批次 reference rewrite 等高風險覆寫操作時，可強制建立快照，不受最短間隔限制。
- 快照是有限防呆機制，用於救回 autosave 誤存壞內容或短時間內誤刪內容，不取代 Git 或正式版本控制。
- `.snapshots/` 預設不納入全文搜尋索引與 Workspace 檔案樹顯示，但需在設定中提供「開啟快照資料夾」捷徑。

## 4.8 外部檔案變更與衝突處理

Filesystem watcher 採 **Workspace root recursive watcher**：

- 監聽 Workspace root 下 relevant file create / modify / delete / rename 事件。
- 事件需做 debounce / coalescing，避免同一次 editor save 產生多個重複事件。
- 未開啟文件若發生外部修改，需更新 Workspace Tree、metadata cache 與全文搜尋索引。
- 已開啟文件的外部修改依 dirty state 決定處理方式。
- 系統以 `saveGeneration`、lastKnownDiskHash 與實際 disk hash 綜合辨識 self-write，不得單純以「幾百毫秒內的 watcher event 一律忽略」作為唯一判定。

### Editor 無未儲存內容

- 可提示後自動 reload，或依設定自動 reload。
- reload 後更新 ProseMirror Document、lastKnownDiskHash 與 FileFormatProfile（若外部工具同時改變 encoding / EOL）。

### Editor 有未儲存內容

停止 autosave，顯示：

- 重新載入磁碟版本。
- 保留 Editor 版本並覆蓋。
- 比較差異。
- 另存副本。

選擇「保留 Editor 版本並覆蓋」前必須再次確認最新 disk hash，並強制建立 snapshot。

## 4.9 匯出

### 單檔匯出

- 輸出 `.md`。
- Export 預設以 UTF-8（無 BOM）+ LF 產生，以提高跨平台可攜性。

### Workspace 匯出

- 輸出 ZIP，保留資料夾結構與 assets。
- ZIP 內由本系統重新序列化的 Markdown 預設為 UTF-8（無 BOM）+ LF。

### Export Mode

**strict**

- 僅輸出本系統宣告支援的 CommonMark + GFM 子集，依 3.7 節 Canonical Style Guide 序列化。

**htmlCompat**

- 允許 `<u>` / `<details>` 等白名單 HTML extension。

若文件含無法降級的 extension，匯出前必須提示，不得 silently drop。

# 5. 資料結構設計

## 5.1 App Global Settings

存放於 Tauri 專屬 App Config Directory。

範例：

```json
{
  "version": 1,
  "theme": "system",
  "recentWorkspaces": [],
  "defaults": {
    "lineBreakMode": "soft",
    "softBreakSerialization": "space",
    "autoSaveDebounceMs": 1500,
    "exportMode": "strict",
    "remoteResourcePolicy": "block",
    "openFolderFileFormatPolicy": "preserve",
    "snapshotRetentionCount": 5,
    "snapshotMinIntervalMs": 60000
  }
}
```

不得包含文件正文。

## 5.2 Workspace Settings

Workspace 根目錄可建立：

```text
.editor-config.json
```

範例：

```json
{
  "version": 1,
  "workspaceName": "Notes",
  "settings": {
    "lineBreakMode": "soft",
    "softBreakSerialization": "space",
    "autoSaveDebounceMs": 1500,
    "exportMode": "strict",
    "openFolderFileFormatPolicy": "preserve"
  },
  "ui": {
    "expandedFolders": [],
    "lastOpenedFile": null,
    "openTabs": []
  }
}
```

規則：

- 不保存完整 `fileTree`。
- 不保存 Markdown 正文。
- 不保存可與 filesystem 產生競爭關係的文件資料。
- `openTabs` 僅為 UI convenience，不是文件內容持久化來源；v1 不保證 crash 後復原未儲存 buffer。

## 5.2.1 Schema Migration 原則

- `version` 欄位代表設定檔 schema 版本，每次 schema 有不相容變更時遞增。
- 若 `version` 低於目前應用程式支援版本，執行對應 migration function 逐版升級。
- 若 `version` 高於目前應用程式支援版本，例如使用者用新版建立、又以舊版開啟：
  - Workspace 仍應能開啟。
  - App 可使用安全預設值作為 runtime fallback。
  - 必須提示使用者「設定檔版本較新，部分偏好設定僅以預設值執行」。
  - **原設定檔進入 read-only compatibility state，不得由舊版 App 自動覆寫。**
  - 若使用者嘗試修改 Workspace 設定，需提示先升級 App，或另存新的相容設定檔；不得直接破壞較新版 schema。
- 設定檔本身損毀或無法解析（非版本問題，例如 JSON 格式錯誤）時，備份損毀檔案為 `.editor-config.json.corrupt`，並以全新預設設定繼續運作，不得阻擋 Workspace 開啟。

## 5.3 Page Metadata

```yaml
---
title: 頁面標題
createdAt: 2026-08-07
tags: [tag1, tag2]
---
```

Front-matter 為選配；不存在時不得強制加入。非 YAML 格式依 4.6.1 處理。

## 5.4 Runtime Document State

建議最低資料結構：

```ts
interface DocumentRuntimeState {
  path: string;
  editorState: unknown;
  dirty: boolean;
  lastKnownDiskHash: string | null;
  fileFormatProfile: FileFormatProfile;
  saveGeneration: number;
}

interface FileFormatProfile {
  encoding: string;
  bom: "none" | "utf8";
  eol: "lf" | "crlf" | "cr";
}
```

規則：

- `FileFormatProfile` 是 runtime save policy，不得取代 filesystem 上的實際檔案。
- Open Folder 讀取時建立 profile；外部 reload 後應重新偵測。
- Import / New File 直接建立 UTF-8 / no BOM / LF profile。
- `saveGeneration` 僅用於事件關聯，不得作為唯一的 external-change 判定依據。

# 6. 安全性設計

## 6.1 Filesystem Scope

所有一般文件操作限定於目前 Workspace root。

寫入前必須進行 canonical path 驗證，避免：

```text
../../outside-workspace
```

等 path traversal。

## 6.2 Symbolic Link

- 預設不得透過 symlink 寫入 Workspace root 以外的位置。
- rename / move / asset rewrite / export 前同樣要套用 canonical path 與 symlink boundary 驗證。
- 若未來開放外部 symlink 寫入，必須由使用者明確允許。

## 6.3 Raw HTML

Markdown 中的 raw HTML：

- 預設不執行。
- 僅允許明確白名單標籤。
- 移除 `<script>`、event handler 及危險 URL scheme。
- 白名單 HTML extension 的 parser / renderer 與安全過濾規則必須有獨立測試。

## 6.4 Remote Resource

為符合 local-first / offline 原則：

```md
![image](https://example.com/a.png)
```

預設只顯示 placeholder，不自動發出 HTTP request。

使用者可對單一資源手動選擇載入；此選擇為單次性，不持久化記住該資源，每次重新開啟文件皆回到預設 placeholder 狀態，避免不小心留下持續對外連線的行為。

## 6.5 Tauri 權限

- 採最小權限原則。
- 前端不得直接取得無限制 filesystem 能力。
- 所有敏感檔案操作透過明確定義的 Rust command。
- 設定 CSP，禁止任意 script 執行與不必要的外部連線。

# 7. 非功能性需求

| 項目 | 要求 |
| --- | --- |
| 效能 | 500+ Markdown 檔案 Workspace 可流暢導覽；Sidebar 應使用 lazy / virtualized rendering |
| 大檔案 | 單一 Markdown >5MB 時使用非同步處理，不阻塞 UI，並顯示 loading / progress 狀態 |
| 相容性 | Windows / macOS / Linux |
| 資料安全 | 文件預設僅存在本機，不上傳至遠端 |
| 可攜性 | Workspace 可直接使用 Git 管理，可由一般文字編輯器讀寫 |
| 可靠性 | 儲存必須採 atomic write、expected hash recheck 與 conflict protection，避免半寫入與外部版本被覆蓋 |
| 無障礙 | 核心功能可由鍵盤操作；使用語意化 HTML 與 ARIA |
| 離線能力 | 預設不需網路即可完成開啟、編輯、儲存、匯入、匯出與全文搜尋 |
| 搜尋效能 | 500+ 檔案 Workspace 的全文搜尋，輸入後結果顯示延遲需在 300ms 內 |
| 外部變更一致性 | Recursive watcher 偵測到未開啟文件的變更後，Workspace Tree 與 Search Index 應在合理時間內同步更新 |

效能驗收需使用固定 benchmark dataset 與固定測試機規格；不可僅以主觀「感覺不卡」判定。

# 8. 測試策略

## 8.1 Round-trip Fixture Test

建立固定測試檔：

```text
tests/fixtures/
  headings.md
  inline-formatting.md
  soft-break.md
  hard-break.md
  nested-list.md
  nested-list-two-digit-marker.md
  loose-vs-tight-list.md
  ordered-list-custom-start.md
  task-list.md
  table.md
  table-cjk-emoji.md
  code-block.md
  links-images.md
  blockquote.md
  frontmatter.md
  frontmatter-toml.md
  html-compat.md
  unsupported.md
  reference-style-link-unsupported.md
  encoding-big5.md
  encoding-gbk.md
  encoding-shiftjis.md
  encoding-utf8-bom.md
  mixed-line-endings.md
```

每次功能更動都必須執行完整 round-trip。

## 8.2 Unit Test

至少涵蓋：

- MDAST -> ProseMirror mapping。
- ProseMirror -> MDAST mapping。
- MDAST Node Allowlist Validator。
- Canonical Style Guide 各項參數的序列化輸出（3.7 節逐項驗證）。
- Soft Break -> space / Hard Break -> backslash mapping。
- list content-column indentation，含 1 位數與 2 位數以上 ordered marker。
- loose / tight semantic preservation。
- table alignment semantics 與 Unicode display width。
- path normalization。
- image path rewrite。
- rename / move relative link recalculation。
- front-matter parser（含非 YAML 格式的降級處理）。
- FileFormatProfile encode / decode。
- 編碼偵測（Big5 / GBK / Shift-JIS / UTF-8 BOM）。
- export downgrade。

## 8.3 Integration Test

至少涵蓋：

- 開啟 Workspace。
- 新增 / rename / move / delete。
- rename / move 時 outgoing link rewrite 與 incoming link confirmation flow。
- autosave debounce。
- atomic write。
- autosave 在 replace 前的第二次 disk hash 驗證。
- 快照備份寫入、throttle 與輪替（4.7.1）。
- external modification conflict。
- recursive watcher 對已開啟與未開啟檔案的事件處理。
- 未開啟檔案外部修改後 Search Index 增量更新。
- Open Folder 原 encoding / BOM / EOL preserve。
- 不可表示字元時阻止 lossy save 並提示轉 UTF-8。
- Import Folder。
- Export ZIP。
- 多分頁同時編輯與各自 autosave 隔離性。

## 8.4 Security Test

至少涵蓋：

- `../` path traversal。
- symlink escape。
- rename / move 經 symlink 導向 Workspace 外部。
- `<script>` / `onerror=` HTML injection。
- 危險 URL scheme。
- 遠端圖片不得在預設情況發出 request。

## 8.5 CI

CI 至少執行：

1. TypeScript type check。
2. Rust test / clippy。
3. Frontend unit tests。
4. Round-trip fixtures。
5. Property-based / fuzz round-trip tests（設定固定 seed 或輸出 failing seed）。
6. Import integration tests。

Round-trip 測試失敗時，不得合併會影響 editor schema、MDAST adapter、serializer、allowlist 的修改。

## 8.6 Property-based / Fuzz Round-trip Test

固定 fixture 無法覆蓋所有「巢狀清單 + inline mark + link + table + code」組合，因此 v1.3 加入生成式測試。

最低要求：

- 從「v1 supported AST schema」隨機生成合法 MDAST，經 `MDAST -> ProseMirror -> MDAST -> Markdown -> parse` 後比較 normalized semantic AST。
- 生成器不得產生 3.4 明確不支援的 node；unsupported 測試另走專用 corpus。
- 對 RawMarkdownNode 隨機生成包含符號、Unicode、空白與換行的 raw text，驗證 preservation path 不改變內容。
- Fuzz failure 必須輸出可重現 seed 與最小化案例，並轉存為新的 regression fixture。
- CI 可限制執行時間，但 release candidate 應跑較大樣本數。

# 9. 開發階段規劃

## Phase 0：Markdown Round-trip PoC

**目標：先驗證最大技術風險。**

工作：

- 建立 remark / MDAST pipeline，套用 3.7 節 Canonical Style Guide 參數。
- 建立 MDAST Node Allowlist Validator。
- 建立 MDAST <-> ProseMirror adapter。
- 完成 3.2 所有語法 fixture，含 Soft / Hard Break、loose / tight list、自訂起始編號、多位數 ordered list 與 CJK table 案例。
- 驗證 unsupported syntax preservation（含 TOML front-matter、reference-style link）。
- 建立 Semantic AST equality helper。
- 建立自動化 round-trip test 與初版 property-based test。

**Exit Criteria：** 所有 MVP 支援語法可穩定 semantic round-trip，且 RawMarkdownNode preservation 邊界已驗證後，才進入 UI 大規模開發。

## Phase 1：核心編輯器與可靠單檔儲存

- Tauri + React + TypeScript 專案骨架。
- 整合 Tiptap。
- 單檔案開啟 / 編輯 / 儲存。
- FileFormatProfile 建立與 Open Folder preserve save。
- dirty state。
- autosave debounce。
- `lastKnownDiskHash` 基本版本檢查。
- temporary file + atomic write。
- atomic replace 前第二次 expected hash 驗證。
- 基本 snapshot 寫入 / throttle。
- 基本快捷鍵與 toolbar。

## Phase 2：Workspace 與檔案管理

- Sidebar tree。
- 新增 / rename / move / delete。
- Open Folder。
- rename / move 相對 link rewrite flow。
- Workspace 設定（含 schema migration 與 future-version read-only compatibility）。
- Workspace root recursive filesystem watcher。
- watcher self-write detection / event coalescing。
- 完整 conflict handling。
- 多分頁編輯模型。

## Phase 3：Import 與 Asset

- Import Folder pipeline。
- 編碼偵測與正規化（含 Big5 / GBK / Shift-JIS）。
- UTF-8 / LF normalize。
- front-matter preservation（含非 YAML 降級處理）。
- asset hash storage。
- asset 垃圾回收工具。
- unsupported syntax report。
- import report UI。

## Phase 4：安全性與格式穩定性

- RawMarkdownNode / Compatibility Mode（含可編輯性與復原流程）。
- HTML whitelist。
- CSP。
- path traversal / symlink protection。
- remote resource blocking。
- 擴充 edge-case tests。
- 強化 property-based / fuzz regression corpus。

## Phase 5：搜尋、匯出與產品化

- 全文搜尋索引與 UI。
- recursive watcher 驅動的索引增量更新。
- 單檔 export。
- Workspace ZIP。
- strict / htmlCompat 模式。
- UX / accessibility。
- 效能優化。
- Packaging / release build。

# 10. Definition of Done

v1.3 Release 必須符合以下條件：

1. 所有 3.2 / 3.3 宣告支援的 Markdown 語法，固定 fixture 的 semantic round-trip 測試通過率為 100%，且輸出符合 3.7 Canonical Style Guide。
2. MDAST allowlist 能阻止未支援節點被誤當成 supported syntax 進入一般 serializer。
3. RawMarkdownNode 與未修改 YAML front-matter 符合 3.6.1 Raw Preservation Guarantee，不得在無警告情況下改寫或遺失。
4. 任意符合支援範圍的 `.md`，經「開啟 -> 編輯器載入 -> 儲存」後不得產生語意失真，且僅允許 3.6.3 明訂的非語意差異。
5. Loose / Tight List 的語意狀態不得非預期翻轉；巢狀清單在 1 位數、2 位數以上 ordered marker 下均產生合法 CommonMark 縮排。
6. Soft Break 依 3.2.1 固定正規化為空白；Hard Break 必須 round-trip 為行尾反斜線語法。
7. Table alignment semantics、欄位數與 cell 內容不得改變；CJK / emoji pretty-print 在固定函式庫版本下輸出可重現。
8. 批次匯入 100 個含巢狀資料夾的 Markdown 檔案後，目標 Workspace 目錄結構符合匯入規則，且至少正確處理 Big5、GBK / GB2312、Shift-JIS、UTF-8 BOM 測試檔。
9. 同名圖片不得互相覆蓋；Missing Asset 不得造成原 Markdown link 被刪除。
10. Front-matter 未修改時不得因開啟 / 儲存而刪除未知欄位；非 YAML front-matter 依 4.6.1 保留或進入 Compatibility Mode。
11. Open Folder 對既有檔案預設保留來源 encoding / BOM / EOL；若新增字元無法由來源編碼表示，必須阻止 lossy save 並提示轉 UTF-8。
12. Autosave 在 atomic replace 前必須再次驗證 expected disk hash；發現外部修改時不得自動覆蓋外部版本。
13. 寫入過程中即使應用程式於 save 中異常中止，也應透過 temporary file + atomic replace 盡可能避免留下半寫入 Markdown；snapshot throttle / retention 必須正常運作。
14. Workspace root recursive watcher 能正確處理 create / modify / delete / rename；未開啟檔案發生外部修改時，Workspace Tree 與全文搜尋索引必須更新。
15. App 自身 atomic save 不得被穩定地誤判為 external conflict；self-write 判定不得只依賴固定時間窗忽略事件。
16. Rename / Move 後，被移動文件的 outgoing local references 應維持指向原目標；incoming references 若需要批次改寫，必須經使用者確認並採 atomic / snapshot / conflict-safe 寫入。
17. Workspace 設定檔版本高於 App 支援版本時，不得被舊版 App 覆寫。
18. 預設情況不主動載入遠端圖片或發送文件內容至網路。
19. 可在完全離線環境下完成核心的開啟、編輯、儲存、匯入、匯出與全文搜尋。
20. Workspace 可直接由 Git 或任意文字編輯器操作；重新開啟本 App 後能依 filesystem 正確反映變更。
21. 應用程式核心不含 AI 功能與 AI API 相依性。
22. 全文搜尋在固定 benchmark 的 500+ 檔案 Workspace 中，輸入後 300ms 內顯示結果。
23. 孤兒 Asset 掃描工具能正確列出未被引用的資源，且不自動刪除任何檔案。
24. Property-based / fuzz test 在 release pipeline 的既定樣本數內無 semantic round-trip failure；任何歷史 fuzz failure 均已固化為 regression fixture。
25. 正常關閉應用程式時不得無提示遺失 dirty tab；非正常終止的 unsaved-buffer recovery 不作為 v1 Release Guarantee。

# 11. MVP 最終產品邊界

MVP 的成功標準不是功能數量，而是以下三件事：

1. Markdown 不被破壞。
2. 檔案完全屬於使用者，可直接離開本 App 使用。
3. 提供比純文字 Markdown Editor 更接近 Notion 的編輯體驗，但不犧牲格式可攜性。

任何新功能若會破壞以上原則，必須延後到 v2 或以可選 extension 形式實作。

# Local MD v1.1.2 更新紀錄 — 2026-08-19

## 本次版本重點

本次 `v1.1.2` 更新完成 Local MD 的視覺定稿、深淺色主題、程式碼區塊、文件縮放、工作區檔案移動與多項桌面互動穩定性修正。設計以瑞士現代主義為主軸，保留黑、白、灰與 `#FF3B30` 紅色作為唯一強調色。

## 介面與視覺

- 統一 Swiss Editorial 風格：嚴格對齊、扁平色塊、細分隔線、左對齊排版。
- 移除舊 LINE 主題與多餘的玻璃、漸層和陰影效果。
- 主色統一為 `#FF3B30`，用於選取狀態、儲存狀態、游標、品牌與互動提示。
- 更新桌面 App 圖示及各平台尺寸資源。
- 工作區標誌改為外框資料夾；懸停時以紅色填充並顯示由左至右的紅色底線。
- 工作區名稱固定顯示原名稱，懸停時以小標籤提示「開啟或切換工作區」，避免改變標題視覺位置。
- 精簡頁面與卡片入場動畫，保留按鈕原有的按壓回饋。
- 修正側邊欄邊界縫隙、收合殘影、位置偏移與多處 Hover 抖動。
- 搜尋、重新整理、分頁批次操作、頂部工具按鈕與齒輪選單改用與編輯工具列一致的彈簧 Hover／按壓動畫；側邊檔案樹的文字與圖示維持固定位置以消除抖動。
- 頂部按鈕的外框與版面位置固定，只讓內部 SVG 從中心縮放；亮色使用淡灰反白，深色使用 `#292929`，圖示顏色不變。

## 設計色票

- 強調紅：`#FF3B30`
- 亮色畫布：`#FBFBFA`
- 亮色側邊欄：`#F3F3F1`
- 亮色分隔線：`#D9D9D9`
- 深色畫布：`#161616`
- 深色頂部與工具列：`#111111`
- 深色 Hover：`#292929`
- 深色分隔線：`#363636`

## 深色與淺色模式

- 新增完整深色模式，程式介面、編輯器、側邊欄、分頁、選單與對話框同步切換。
- 歡迎畫面右上角可在開啟工作區前切換深／淺色。
- 太陽與月亮切換具備短促淡入、旋轉和縮放動畫。
- 歡迎畫面選擇的主題會延續到進入工作區，不再被舊工作區設定覆蓋。
- macOS 使用 Overlay 標題列，使 App 背景延伸至紅黃綠視窗按鈕區域。
- `v1.1.1` 修正 Overlay 標題列頂部無法拖曳視窗的問題。
- `v1.1.2` 恢復固定顯示工作區名稱，並以小標籤提示可開啟或切換工作區。

## 編輯器與程式碼區塊

- 新增 Monokai Soda 風格程式碼區塊。
- 程式碼區塊右上角以精簡文字顯示目前類型。
- 支援 Plain Text 與 Code 模式。
- Code 模式支援 Python（預設）、JavaScript、Bash、Rust 等語言語法高亮。
- 改善行內程式碼在深色模式的辨識度。
- 程式碼類型選單整合為單一下拉選單，避免遮住內容。
- 新增編輯工具列快捷鍵提示與鍵盤快捷鍵說明視窗。

## 文件縮放

- 編輯工具列右側新增 `− / 百分比 / ＋` 文件文字縮放控制。
- 縮放範圍為 70%–160%，預設 100%。
- 支援 `⌘/Ctrl +`、`⌘/Ctrl -` 與 `⌘/Ctrl 0`。
- 支援 `⌘/Ctrl + 滾輪／觸控板`，並加入累積門檻與節流，避免一次跳動過多。
- 縮放設定儲存在工作區設定中，不會改變視窗實際尺寸。

## 工作區與檔案管理

- 文件選單新增「移動到資料夾」。
- Markdown 文件可直接拖曳到側邊欄資料夾。
- 可拖到「檔案・根目錄」移回工作區根目錄。
- 拖曳時顯示紅色落點提示，移動後自動展開目的資料夾。
- 移動或重新命名後會同步更新已開啟分頁、分頁群組與 Markdown 相對連結。
- 點擊檔案列表空白處或「檔案・…」可回到工作區根目錄，新增文件不再被鎖在先前選取的子資料夾。
- 點擊頂部工作區名稱或資料夾圖示，可重新選擇並切換工作區。
- 工作區切換入口具備紅色資料夾填充、紅色底線與小型操作提示標籤。

## 分頁與穩定性

- 修正只有一個分頁時開啟第二個分頁造成的全視窗閃爍與輕微震盪。
- 調整分頁列高度與溢位行為，隱藏不必要的水平與垂直捲軸。
- 分頁名稱空間不足時會逐步縮短，避免破壞工具列布局。
- 修正設定按鈕被遮住、選單定位與深色模式配色問題。
- 保留檔案內容儲存、衝突偵測、工作區索引與離線優先行為。

## 打包前驗證

- [x] 前端 TypeScript 與 Vite 正式建置通過
- [x] 前端單元測試通過（62 項）
- [x] Rust 單元測試通過（15 項）
- [x] Tauri Debug 桌面建置通過
- [ ] 淺色模式歡迎畫面與工作區一致
- [ ] 深色模式歡迎畫面、工作區與 macOS 頂部一致
- [ ] 拖放文件到資料夾及根目錄正常
- [ ] `⌘ + 滾輪` 與工具列縮放正常
- [ ] 開啟第二個分頁無閃爍或震盪
- [x] 正式打包並覆蓋 `/Applications/Local MD.app`（v1.1.2）

## 備註

- 建置時仍會顯示編輯器 bundle 超過 500 kB 的效能提示；這是非阻斷警告，不影響目前打包與功能。
- 正式 App `v1.1.2` 已安裝至 `/Applications/Local MD.app`；上一版 `v1.1.1` 備份於 `/private/tmp/Local MD.app.pre-v1.1.2-20260819`，`v1.1.0` 備份於 `/private/tmp/Local MD.app.pre-v1.1.1-20260819`。
- Release `.app` 位於 `src-tauri/target/release/bundle/macos/Local MD.app`；本次 `.dmg` 製作步驟未完成，但不影響已安裝的 App。
- 本文件為 2026-08-19 收工版本；後續打包應以目前工作區內容為準。
